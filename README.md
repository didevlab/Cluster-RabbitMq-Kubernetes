# Cluster-RabbitMq-Kubernetes
## Run following exactly the sequence below if you don't splinter :)
- kubectl apply -f rabbitmq_rbac.yaml
- kubectl apply -f rabbitmq_configmap.yaml
- kubectl apply -f rabbitmq_service.yaml
- kubectl apply -f rabbitmq_rbac.yaml

### psicopante